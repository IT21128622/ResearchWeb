"use client";

import { useRouter } from "next/navigation";

const ContactUs = () => {
  return (
    <section
      id="footer"
      className="py-4 flex items-center px-5 justify-center bg-black-200"
    >
      <h1 className="font-semibold text-sm">
        Cric
        <span className="text-pink-600">.</span>
        Boost
      </h1>
    </section>
  );
};

export default ContactUs;
